%p
  = current_user.name if current_user.present?

- if @trends.present?
  - @trends.each do |t|
    %p=t.name

-if current_user.present? && @trends.blank?
  :javascript
    $(function(){
      $.ajax({
          url: '#{trends_path}',
          cache: true,
          type: 'GET'
      });
    });